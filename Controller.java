/**
 * 
 * @author Nick
 * @version Controller
 * 
 * I had previously finished the project when I realized that MVC structure was required so
 * I implemented it as well as I could with but my model and view are a little smushed 
 * together and a controller wasn't necessarily essential so it is passed as null to
 * the View and the Models
 */
public class Controller 
{
	public Controller()
	{
		/*Model m = new Model(this);
		Model2 m2 = new Model2(this);
		do {
		if(m.win())
		{
			new View(0, this);
		}}while(m.win());*/
	}
}
